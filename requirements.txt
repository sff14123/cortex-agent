# Cortex MCP 서버 의존성
# 설치: pip install -r requirements.txt

pyyaml>=6.0
requests>=2.31.0
numpy>=1.24.0
python-dotenv>=1.0.0
sentence-transformers>=2.2.0

# 벡터 검색 엔진 (하이브리드 검색)
sqlite-vec>=0.1.2
kuzu>=0.11.3
torch==2.5.1
openai>=1.0.0
tqdm>=4.66.0
pypdf>=3.0.0

# 워처 데몬(파일 감시)
watchdog>=3.0.0